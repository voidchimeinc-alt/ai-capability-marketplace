# Piton — Agent Context

Read `PRODUCT.md`, `ARCHITECTURE.md`, `DECISIONS.md`, and `ENGINEERING_RULES.md` before changing product behavior.

This is an **AI Capability Marketplace** (working name: Piton), not a tools directory or freelancer site.

Next.js version docs: `node_modules/next/dist/docs/`.

## Commands

```bash
npm run dev
npm run lint
npm run typecheck
npm run test
npm run build
npm run seed:check
```

## Brand

Change the product name in `src/lib/brand.ts` only.
